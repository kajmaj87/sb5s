[package]
name = "lua_engine"
version = "0.1.0"
edition = "2024"

[dependencies]
logic = { path = "../logic" }
mlua = { version = "0.10.3", features = ["luau", "serialize", "send", "error-send"] }
walkdir = "2.5.0"
