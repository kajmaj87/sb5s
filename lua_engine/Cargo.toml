[package]
name = "lua_engine"
version = "0.1.0"
edition = "2024"

[dependencies]
logic = { path = "../logic" }
parking_lot = "0.12.3"
mlua = { version = "0.10.3", features = ["luau", "async", "serialize", "send", "error-send"] }
walkdir = "2.5.0"
